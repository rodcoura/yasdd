---
description: "Start a lean SDD feature: elicitation -> architecture (with self-check + batches + testing) -> gate -> implement by component -> test -> verify."
---
# /yasdd

Load the skill `yasdd` (skill tool) and follow it.

## Arguments

$ARGUMENTS
